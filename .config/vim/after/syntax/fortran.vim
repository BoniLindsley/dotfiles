" Disable the highlight past line 72.
syntax clear fortranSerialNumber

" Use syntax folding if folding is enabled.
let fortran_fold = 1
let fortran_fold_conditional = 1
let fortran_fold_multilinecomments = 1
